/// App version surfaced in UI (e.g. the terminal `version` command).
///
/// Keep in sync with the `version:` field in `pubspec.yaml`.
class AppVersion {
  AppVersion._();

  static const String current = '1.0.0';
}
