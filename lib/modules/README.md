# modules/

Placeholder folder.

> Note: per the project architecture guide, self-contained "nested modules"
> live under `features/<feature>/modules/<ModuleName>/` (e.g.
> `features/dashboard/modules/report/`). This top-level folder is available
> for anything that doesn't belong to a specific feature.
