# screens/

Top-level screens for the inventory masters module.

- `inventory_masters_screen.dart` — index screen that lists the masters as
  tappable cards, each opening its sub-module screen (e.g. stock group,
  stock category) as a pushed route.
